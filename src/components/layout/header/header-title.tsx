import { Link } from 'react-router-dom';

const HeaderTitle = () => {
	return (
		<div style={{ fontSize: '3.5vw' }}>
			<Link to='/'>Jolly Ping Pong!</Link>
		</div>
	);
};

export default HeaderTitle;
